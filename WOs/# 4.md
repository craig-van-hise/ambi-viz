The layout for `TransportControls.tsx` is broken. It is currently overflowing the central area and overlapping the sidebars. 

Please perform the following fix:
1. Examine the layout structure in `App.tsx`. The `<TransportControls />` must be constrained to the central viewport area, between the Left and Right sidebars.
2. If the sidebars are absolute/fixed, update the TransportControls container style to use a width calculation that subtracts the sidebar widths (e.g., width: calc(100% - leftWidth - rightWidth)).
3. Ensure that when sidebars are toggled hidden, the TransportControls correctly expand to fill the new space.
4. Check `src/components/TransportControls.tsx` and ensure it is not using `position: fixed` or `width: 100vw` which would ignore the parent's constraints.

Verify the fix by checking that the transport 'pills' remain centered within the visible grid area of the MainViewer and do not overlap the sidebars.