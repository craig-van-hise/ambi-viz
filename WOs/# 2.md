The blueprint is approved with the following mandatory requirements for the file system execution:

**1. Component Atomization (Right Sidebar)**
Instead of one large sidebar file, create a `src/components/panels/` directory and extract the accordion sections into these specific files:
- `ViewAndTrackingPanel.tsx` (Top accordion section)
- `CameraControlPanel.tsx` (Handles the Position/Orientation conditional logic)
- `VisualizerSettingsPanel.tsx` (Handles the Volumetric/Sphere parameter logic)
- `ESKFTuningPanel.tsx` (Handles ESKF sliders)
- `ControlSidebar.tsx` will now just be a slim wrapper that imports and renders these panels.

**2. Standalone Modals**
Create `src/components/InfoModal.tsx` and `src/components/SettingsModal.tsx`. 
- InfoModal should be imported/triggered by the Header.
- SettingsModal should be imported/triggered by TransportControls.

**3. The Main Viewer**
Extract the central 3D canvas into `src/components/MainViewer.tsx`. 
*Constraint:* Ensure the `containerRef` is passed or handled so that the `AmbiScene` instance in `App.tsx` correctly attaches to the DOM element without re-initializing or flickering.

**4. The Rest of the Layout**
- Extract `src/components/Header.tsx` (with the 3 layout toggle buttons).
- Extract `src/components/TrackQueue.tsx` (Left panel).
- Extract `src/components/TransportControls.tsx` (Bottom panel).
- Extract `src/components/Footer.tsx` (Status bar).

**Execution:**
Execute these changes now. Provide a unified diff for `App.tsx` and show the contents of the new component files. Maintain all existing state logic and service orchestrations (AudioEngine, AmbiScene, HeadTracking) within App.tsx.