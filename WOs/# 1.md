I am refactoring a 1,400-line monolith in `src/App.tsx`. 

**Phase 1: Analysis & Blueprinting**
1. Read `src/App.tsx` and map out its state dependencies (specifically how it manages the AudioEngine, AmbiScene, and HeadTrackingService).
2. Look at the existing `src/components/`, `src/visualizer/`, and `src/audio/` directories to understand the current architecture.
3. Propose a modular component tree that extracts the following:
   - `<Header />`: Title, subtitle, and the 3 layout toggle buttons.
   - `<MainViewer />`: The central square containing the 3D canvas (imports AmbiScene).
   - `<TrackQueue />`: The left sidebar logic.
   - `<TransportControls />`: The bottom playback and settings triggers.
   - `<ControlSidebar />`: The right sidebar, including the dynamic/conditional panels for Camera Position/Orientation and Visualizer Controls.
   - Standalone Modal components for Info and Settings.

**Deliverable:**
Provide a list of the new files to be created. For each file, list:
- The specific React props it will require from App.tsx.
- Any local state that can be moved out of App.tsx into the component.
- The core files it needs to import (e.g., AmbiScene, AudioEngine).

DO NOT write or modify any files yet. Present the plan and wait for my approval.