The build failed with 3 TypeScript errors. Please apply these specific fixes:

1. In `src/components/MainViewer.tsx`, update the `MainViewerProps` interface so `containerRef` is `React.RefObject<HTMLDivElement | null>`.
2. In `src/components/TransportControls.tsx`, update the `activeTrack` prop in the interface so `duration` is `string | undefined`.
3. In `src/components/TrackQueue.tsx`, remove the unused `playbackState` from the component props destructuring.

After applying, verify the fix by running `npm run build` using your shell tool.