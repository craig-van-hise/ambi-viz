# Product Requirements Prompt (PRP) — [Status Marker] ✅ Completed

## 1. Project Context & Objectives
The GitHub Actions CI/CD pipeline is successfully running but failing at the build step (`tsc -b && vite build`) due to strict TypeScript compilation errors. These errors consist of unused imports (`TS6133`), type-only import enforcement (`TS1484`), outdated mock objects in test files (`TS2322`, `TS2741`), and a Framer Motion literal type inference issue (`TS2322`). The objective is to resolve all TypeScript errors to unblock the production build.

## 2. Technical Decisions & Dependencies
* **Target Files:** Various files across `src/components/` and `src/components/onboarding/`.
* **Architectural Action:** 1. Remove all unused `import React from 'react'` statements (React 17+ JSX transform makes them obsolete).
  2. Update outdated test mocks to match their current component interfaces.
  3. Correct type inference for the `transition` prop in `framer-motion`.

## 3. Implementation Phases

### Phase 1: Unused Variables & Type Imports — [Status Marker] ✅ Completed
* **Objective:** Clear out all `TS6133` and `TS1484` strict mode errors.
* **Instructions:**
  1. **Remove `import React from 'react';`** (or ensure `React` is removed if destructuring other hooks) from the following files:
     * `src/components/ControlSidebar.test.tsx`
     * `src/components/InfoModal.test.tsx`
     * `src/components/onboarding/HeadphonePrompt.tsx`
     * `src/components/onboarding/OnboardingContext.tsx`
     * `src/components/onboarding/__tests__/HeadphonePrompt.test.tsx`
     * `src/components/onboarding/__tests__/OnboardingContext.test.tsx`
     * `src/components/onboarding/__tests__/OnboardingSteps.test.tsx`
     * `src/components/panels/ViewPanel.test.tsx`
  2. **`InfoModal.test.tsx`**: Remove the unused `getByText` variable from the render destructuring.
  3. **`OnboardingSteps.test.tsx`**: Remove the unused `rerender` variables from the render destructuring (lines ~102, 147).
  4. **`OnboardingContext.tsx`**: Change the import of `ReactNode` to a type-only import: `import type { ReactNode } from 'react';`.
* **TDD Checkpoint:**
  * **Status Update:** "Transition Phase status to 🔵 Active. Execute the removals. Update status to ✅ Completed before initiating subsequent phases."

### Phase 2: Fix Outdated Test Mocks — [Status Marker] ✅ Completed
* **Objective:** Fix type mismatch errors in the test suite (`TS2322`, `TS2741`).
* **Instructions:**
  1. **`ControlSidebar.test.tsx`**: Locate the mock `defaultProps` object. The `cameraUIState` property is missing the new orientation axes. Update it to include:
     ```javascript
     cameraUIState: { x: 0, y: 3.55, z: 3.7, yaw: 0, pitch: 0, roll: 0 },
     ```
     *(Remove `rx, ry, rz` if they are present in the mock but no longer in the interface).*
  2. **`OnboardingSteps.test.tsx`**: Locate the mock context provider values (around line 214). It currently has `restartOnboarding`. Rename this key to `resetOnboarding` to match the `OnboardingContextProps` interface.
* **TDD Checkpoint:**
  * **Status Update:** "Transition Phase status to 🔵 Active. Apply the mock updates. Update status to ✅ Completed before initiating subsequent phases."

### Phase 3: Fix Framer Motion Type Inference — [Status Marker] ✅ Completed
* **Objective:** Fix the `TS2322` error in `AnimatedPointer.tsx` regarding the `ease` string.
* **Instructions:**
  1. Open `src/components/onboarding/AnimatedPointer.tsx`.
  2. Locate the `transition={{ ... }}` prop on the `motion.div`.
  3. TypeScript is inferring the `ease` string generically. Cast the ease property `as const` (e.g., `ease: "easeInOut" as const`) or cast the entire transition object `as any` (e.g., `transition={{ repeat: Infinity, duration: 1, ease: "easeInOut" } as any}`) to bypass the strict literal check.
* **TDD Checkpoint:**
  * **Status Update:** "Transition Phase status to 🔵 Active. Apply the type cast. Update status to ✅ Completed before initiating subsequent phases."
  * **User Verification:** "[USER VERIFICATION REQUIRED] In your local terminal, run `npm run build`. Verify that the build succeeds without any TypeScript errors before committing and pushing to GitHub."

## 4. Final Review & Validation
* **Technical Audit:** Ensure that removing the React imports didn't accidentally remove necessary hooks like `useState` or `useEffect` that were on the same import line.
* **Status Reconciliation:** Verify all sub-phases match ✅ or ❌ before updating the top-level status to ✅.
* **Final Sign-off:** Present the clean build to the user for project completion approval.