
> **Task: Create an Information & Attribution Modal**
> **1. Add the UI Trigger:**
> * Place an `info` Material icon in the top-right header section.
> * Style it to match existing header icons and ensure proper horizontal alignment.
> 
> 
> **2. Create the Modal Component:**
> * Implement a centered modal that appears when the `info` icon is clicked.
> * **Styling:** Use a dark theme (`#121212`) with a semi-transparent, blurred backdrop (`backdrop-filter: blur(8px)`).
> * Include a `close` icon in the top-right and enable "click outside to close."
> 
> 
> **3. Content:**
> * **Heading:** About AmbiViz
> * **Body:** Use the following "What & Why" text:
> *"AmbiViz is an open-source spatial audio player built to solve the accessibility gap in 3DoF listening. While 3D audio usually requires specialized hardware, AmbiViz uses web-based head tracking to let anyone experience Ambisonic mixes with standard headphones. It serves as both a technical visualizer and a showcase for creators to drag-and-drop their own spatial projects."*
> * **Licensing:** A scrollable section titled "Open Source Attribution."
> * **Footer:** A "View Source on GitHub" button.
> 
> 
> **4. Automated Data Retrieval (CRITICAL):**
> * You must crawl the project to generate the attribution list.
> * **Step A:** Read `package.json` to identify all dependencies (e.g., Three.js, React).
> * **Step B:** Search the `node_modules` subdirectories for `LICENSE` or `README` files for each package.
> * **Step C:** If a license isn't found locally, use your internal knowledge to identify the standard license for that library (e.g., Three.js is MIT).
> 
> 
> **5. Execution:**
> * Provide an **Implementation Plan** first.
> * The plan should list every library you found and a mockup of the modal's internal layout before modifying any files.
> 
