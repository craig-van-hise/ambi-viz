# Product Requirements Prompt (PRP) — [Status Marker] ✅

## 1. Project Context & Objectives
The default audio playlist loaded upon application initialization needs to be reordered. The track names and application logic will remain entirely unchanged; only the initial load sequence defined in the playlist manifest needs updating.

## 2. Technical Decisions & Dependencies
* **Target File:** `public/ambisonic_audio_queue/Queue order.txt`
* **Architectural Action:** Overwrite the `Queue order.txt` manifest with the new numbered sequence, utilizing the exact original track titles so the existing `filenameMap` in `App.tsx` continues to resolve the files correctly without any code changes.

## 3. Implementation Phases

### Phase 1: Update the Playlist Manifest — [Status Marker] ✅
* **Objective:** Define the new default load order using the original track names.
* **Instructions:**
  1. Open `public/ambisonic_audio_queue/Queue order.txt`.
  2. Replace its entire contents with the following exact text (maintaining the `1. Title` format regex expected by the parser):
     ```text
     1. 3rd Order Clock
     2. Bach Invention no5 in Eb_AmbiX_3O
     3. A Furiosa (Maxixe)_dry
     4. SMB 2 Theme
     5. Beethoven - String Quartet No 13 in B-flat major - IV Alla danza tedesca
     6. Let Me Tell You About My Boat
     7. Final Fantasy Prelude
     8. TEST-09-DYN-B-SPIRAL_Order3
     ```
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the app initializes, When the remote queue fetches, Assert that the queue populates with 8 tracks in the newly defined order and no 404 network errors occur.]
  * **Status Update:** "Transition Phase status to 🔵 Active. Update the text file. Update status to ✅ Completed before initiating subsequent phases."
  * **User Verification:** "[USER VERIFICATION REQUIRED] Hard refresh the browser. Verify that the Queue sidebar loads the original track names but in the newly requested sequence."

## 4. Final Review & Validation
* **Technical Audit:** Double-check that there are no trailing spaces or typo alterations in the text file strings that might break the dictionary lookup in `App.tsx`.
* **Status Reconciliation:** Verify all sub-phases match ✅ or ❌ before updating the top-level status to ✅.
* **Final Sign-off:** Present the updated queue to the user for project completion approval.