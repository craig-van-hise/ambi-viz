# Product Requirements Prompt (PRP) — [Status Marker] ✅

## 1. Project Context & Objectives
The `InfoModal.tsx` component requires a comprehensive content update to properly communicate the application's mission and technology, while rigorously fulfilling open-source licensing and attribution requirements. This update will incorporate project context, credit for the Google Open Binaural Renderer (OBR), explicit attribution for a bundled audio asset sourced from Freesound, and a structured table of third-party library dependencies.

## 2. Technical Decisions & Dependencies
* **Target File:** `src/components/InfoModal.tsx`
* **Styling Strategy:** Utilize existing Tailwind CSS utilities within the project to ensure structural consistency. The content will be dense, so semantic HTML (`<h3>`, `<p>`, `<ul>`, `<table>`) paired with appropriate spacing (`space-y-4`, `mb-4`) and responsive overflow containers (`overflow-x-auto` for the table) are strictly required.
* **Content Hierarchy:**
  1. **The Mission:** Contextualizing the 3DoF accessibility goal.
  2. **The Technology:** Explaining the 3rd-order spatial mapping and visualizer.
  3. **Acknowledgments:** Highlighting OBR and the specific Freesound audio asset (`3rd Order Ambi Clock Test.opus`).
  4. **Open Source Attribution:** A styled data table mapping dependencies to their authors and licenses.

## 3. Implementation Phases

### Phase 1: InfoModal Content Integration — [Status Marker] ✅
* **Objective:** Refactor the internal structure of `InfoModal.tsx` to display the new informational copy and attribution table without breaking the modal's layout or scrollability.
* **Instructions:**
  1. Open `src/components/InfoModal.tsx`.
  2. Locate the main content container and append/replace the sections to match the following structure:
     * **Heading:** "The Mission"
       * *Body:* "AmbiViz was built to solve the accessibility gap in 3DoF listening. While 3D audio usually requires specialized hardware, AmbiViz uses webcam-based head tracking to let anyone experience Ambisonic mixes with standard headphones."
     * **Heading:** "The Technology"
       * *Body:* "This tool serves as a high-fidelity showcase for creators to drag-and-drop their own spatial projects, mapping 3rd-order Ambisonic energy onto an interactive 3D sphere for real-time analysis."
     * **Heading:** "Acknowledgments"
       * *List Item 1:* "**Google Open Binaural Renderer (OBR)**: Ambisonic decoding and spatialization powered by Google's OBR (Apache-2.0)."
       * *List Item 2:* "**Audio Asset**: '3rd Order Ambi Clock Test.opus' is adapted from [Ambisonic Clock by clucs](https://freesound.org/people/clucs/sounds/428571/) on Freesound."
     * **Heading:** "Open Source Attribution"
       * *Table:* Construct a responsive 3-column table (Library, Author, License) mapping the following data:
         * `@mediapipe/tasks-vision` | mediapipe@google.com | Apache-2.0
         * `@tailwindcss/vite` | Tailwind Labs | MIT
         * `@types/three` | DefinitelyTyped | MIT
         * `ambisonics` | Archontis Politis | BSD-3-Clause
         * `lucide-react` | Eric Fennis | ISC
         * `motion` | Matt Perry | MIT
         * `react` / `react-dom` | Meta | MIT
         * `tailwindcss` | Tailwind Labs | MIT
         * `three` | mrdoob | MIT
  3. Wrap the table in a `<div className="overflow-x-auto">` to ensure horizontal scrolling on narrow viewports, preventing the modal from breaking its bounds. Apply subtle border styling to the table cells (e.g., `border-b border-gray-700/50`, `text-sm`, `text-left`, `p-2`).
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the `InfoModal` is rendered, When the document is queried, Assert that the text "Freesound" and "Google Open Binaural Renderer" are present in the document.]
  * **Test Case 2:** [Given the `InfoModal` is rendered, When querying for a `table` element, Assert that it exists and contains at least 9 data rows.]
  * **Mocking Requirements:** None required for this DOM structural test.
  * **Status Update:** "Transition Phase status to 🔵 Active. Author and run these verification tests. Once tests pass successfully, update status to ✅ Completed before initiating subsequent phases."
  * **User Verification:** "[USER VERIFICATION REQUIRED] Open the AmbiViz application in the browser, trigger the Info Modal, and verify that the typography aligns with the design system, the URLs route correctly, and the table does not overflow the modal container."

## 4. Final Review & Validation
* **Technical Audit:** Inspect the component on a simulated mobile viewport to guarantee the `overflow-x-auto` table wrapper functions correctly and the modal retains its closeability.
* **Status Reconciliation:** Verify all sub-phases match ✅ or ❌ before updating the top-level status to ✅.
* **Final Sign-off:** Present the rendered modal UI to the user for project completion approval.